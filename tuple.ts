/* TUPLE */

let arr: (string|number|boolean)[] = ['Bob', 30, false]
// tuple quy ước đúng vị trí phải là dạng gì, số lượng phần tử ntn
const tup: [string, boolean, number] = ['Bob', false, 7]

const myStudent: [string, number] = ['Alice', 22]